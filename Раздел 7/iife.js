(function() {
  // Код
})(); // Сразу же вызываем функцию
// Без скобок вокруг объявления ф-ции JavaScript не поймет, что мы от него хотим
// Так как функция не имеет имени (анонимная), в window она не попадет